{
  "name": "civicpulse-root",
  "version": "1.0.0",
  "description": "CivicPulse Root Deployment Handler",
  "scripts": {
    "start": "npm --prefix backend start",
    "build": "npm --prefix backend install",
    "backend": "npm --prefix backend start",
    "frontend": "npm --prefix frontend start"
  }
}
